openapi: 3.0.1
info:
  title: Dynamic ${today}
  x-cortex-git:
    github:
      repository: ${org}/${repo}
      alias: ${alias}
  x-cortex-tag: ${environment}-${tenant}-gitops-catalog
  x-cortex-type: service
