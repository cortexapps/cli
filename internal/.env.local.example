# Local dev environment — Cortex running on host via bootRun.
# Copy to .env.local and fill in your API key from ~/.cortex/config [cortex-local].
CORTEX_API_KEY=
CORTEX_BASE_URL=http://host.minikube.internal:8080
CORTEX_APP_URL=http://app.local.getcortexapp.com:3000
CORTEX_TENANT_CODE=cortex-local
