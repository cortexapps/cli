[pytest]
minversion = 9.0.3
markers =
    functional: Functional tests that use CLI infrastructure but test broader scenarios
    setup: Setup tests that run before other tests
    cleanup: Cleanup tests that sweep orphaned resources (run manually)
addopts = -m "not setup and not cleanup"
testpaths =
    tests
