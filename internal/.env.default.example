# Cloud workspace environment — copy to .env.default and fill in your values.
# Each team member's .env.default will differ (their own workspace + API key).
CORTEX_API_KEY=
CORTEX_BASE_URL=https://api.getcortexapp.com
CORTEX_APP_URL=https://app.getcortexapp.com
CORTEX_TENANT_CODE=
