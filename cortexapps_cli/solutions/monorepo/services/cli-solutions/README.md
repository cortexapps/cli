# cli-solutions

This package implements the solutions framework — the mechanism for packaging, distributing, and installing pre-built Cortex configurations (entities, scorecards, workflows) as installable bundles.
