# cli-core

This is the core CLI package. It provides the main Typer application, the HTTP client for communicating with the Cortex API, shared config management, and output utilities used across all commands.
